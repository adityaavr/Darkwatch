# Context
You are an elite, principal-level full-stack engineer and UI/UX expert competing in a high-stakes 2-day hackathon. You are building **"DarkWatch: The Cart Cleanser"**.

Our stack is Next.js 16 (React, App Router), Bun runtime, Vercel AI SDK (`ai-sdk`), TinyFish for browser automation, and Tailwind CSS + shadcn/ui for styling.

# The Application
DarkWatch acts as an "Autonomous Proxy Shopper." The web has heavily militarized against consumers with deceptive UX, hidden junk fees, and dark patterns. DarkWatch sanitizes the checkout experience by autonomously executing clicks, evaluating cart visuals, and un-checking hidden traps.

When a user submits a search query and a target website:
1. **The Watchtower (UI State 1 & 2):** The app drops into a dark, glowing "Hacker Terminal" UI, using Server-Sent Events (SSE) via `ai-sdk` to stream live execution logs as our AI agent browses the web.
2. **The Sanitizer (Backend Action):** The backend coordinates TinyFish. It searches the site, finds the product, navigates to the checkout, and uses multimodal Vision to spot and un-check hidden fees.
3. **The Clean Reveal (UI State 3):** Once sanitized, the UI transitions instantly from the chaotic terminal into an ultra-clean, minimalist, light-mode "Stripe-style" receipt showing the "Junk Fees Stripped" and the final sanitized true price.

# Our Primary Test Scenario (The Demo)
For our MVP demo, we are hardcoding the backend orchestration to execute this exact target:
- **Target Site:** `shein.com`
- **Search Query:** `"Kindle Reader Protective Case, "`

# AI Model Strategy (via Vercel AI SDK)
We are using a dual-model approach to balance speed and intelligence:
- Use `openai('gpt-4o-mini')` for fast, cheap DOM analysis, orchestration steps, and generating the live Terminal SSE logs token-by-token.
- Use `openai('gpt-4o')` (with image/vision support) exclusively at the final checkout step to evaluate the full-page screenshot, locate the visual coordinates/labels of hidden fees (like fake "shipping guarantees"), and output the strict structured payload.

# Technical Directives & Skill Invocations

skills:
- `ai-sdk`
- `tinyfish`
- `Bun`
- search online (webfetch)

Please activate and refer to the following workspace skills to generate the optimal code:

1. **Backend / Environment:** Invoke `@bun`. Ensure all package scripts and Next.js backend API routes (`app/api/...`) use the `edge` or `nodejs` runtimes optimized for Bun.
2. **AI Orchestration:** Invoke `@ai-sdk`. Build the `/api/clean-cart` endpoint. Use `streamText` or `streamObject` with `gpt-4o-mini` to continuously push markdown log updates (e.g., `[TinyFish] Searching shein.com for "Kindle Reader..."`) down to the client via SSE. Hand off to `gpt-4o` for the final Vision phase.
3. **Browser Automation:** Invoke `@tinyfish`. Create `lib/tinyfish-service.ts`. Draft the specific TinyFish commands/prompts required to:
   - Go to `shein.com` and dismiss initial massive coupon pop-ups/cookie banners.
   - Enter `"Kindle Reader Protective Case, "` into the search bar and submit.
   - Click the first relevant product, click 'Add to Cart', and navigate to checkout.
   - Trigger a viewport screenshot and wait for GPT-4o Vision to identify the "Shipping Guarantee" box so TinyFish can execute a click to uncheck it.
4. **UI/UX & Styling:** Invoke `@frontend-design` and `@web-design-guidelines`. 
   - State 1 (Landing): A sleek input card for the URL and Query.
   - State 2 (Action): A dark-mode, glowing, monospaced terminal that beautifully auto-scrolls the `ai-sdk` streams.
   - State 3 (Success): A clean, high-trust "Stripe-style receipt" component that maps the JSON result: Base Price, the specific Junk Fees removed, and Final Total.

# Immediate Tasks
1. Generate `app/page.tsx` that manages the local state transitions between State 1, 2, and 3. Scaffold the basic shadcn/ui elements.
2. Generate `app/api/clean-cart/route.ts` showing how the dual-model Vercel AI SDK pipeline is structured with dummy stream delays to simulate the execution.
3. Generate `lib/tinyfish-service.ts` showing the theoretical function map for our Shein test case.

Write production-grade, hackathon-ready code. Start by stubbing the UI layer and streaming mechanisms first so the frontend illusion is perfect.

task: we need to pivot from gemini to gpt

fix any outdated code & refactor first

my keys are

OPENAI_API_KEY=
TINYFISH_API_KEY=

